server {
    listen 80;
    server_name _;

    location /api/ {
        proxy_pass http://llmops-api:5001;
        rewrite ^/api/(.*)$ /$1 break;
        include proxy.conf;
    }

    location / {
        proxy_pass http://llmops-ui:3000;
        include proxy.conf;
    }
}